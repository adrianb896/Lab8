package hazardControl;

import java.util.List;

public class CEO {
    public class implementDecision<d>(List<Decision> ds) {
    private void sortByPriority(ds) {
        ds = sortByPriority(ds);
    }
    Decision d = ds.get(0);
         = ds.get(1);
        d.execute(this);
}


    public class seeDanger(r:HReporter_IF; h:Hazard) {
        List<Decision> decisions = new List<Decision>;
    for(each e:members) {
            Manager m = (Manager) e;
            decisions.append(m.suggestDecision(h))};
    }
    implementDecision(decisions);
}

